// package com.example.demo.security;

// public class BCryptPasswordEncoder implements PasswordEncoder {

//     @Override
//     public String encode(String rawPassword) {
//         // simple mock encoding (enough for tests)
//         return "ENC(" + rawPassword + ")";
//     }
// }
